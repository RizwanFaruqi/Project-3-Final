public class CountryRunner {
    public static void main(String[] args) {
        CountryGame game = new CountryGame();
        String result = game.gameRunner(); // Capture the returned value
        System.out.println(result);        // Use the result (e.g., print it)
    }
}

